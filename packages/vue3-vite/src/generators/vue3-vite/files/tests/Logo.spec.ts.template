import { shallowMount } from '@vue/test-utils';
import Logo from '../src/app/components/widgets/Logo.vue';

describe('Logo', () => {
  it('renders logo url', () => {
    const wrapper = shallowMount(Logo);
    expect(wrapper.isVisible()).toBe(true);
  })
});
